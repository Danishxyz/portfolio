---
description: Deploy the project to Vercel
---
To deploy the latest version of the project to Vercel:

1. Open a terminal in the project root.
2. Run the deployment command:
   ```bash
   npx vercel --prod
   ```
3. If prompted, confirm the default settings.
4. The command will output the production URL upon completion.
